<Window
  x:Class="VsLocalizedIntellisense.Views.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  
  xmlns:p="clr-namespace:VsLocalizedIntellisense.Properties"
  
  Title="{x:Static p:Resources.ApplicationName}"
  Height="450"
  Width="800"
  WindowStartupLocation="CenterScreen"

  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:datacontext="clr-namespace:VsLocalizedIntellisense.ViewModels" d:DataContext="{d:DesignInstance Type=datacontext:MainViewModel}"
  mc:Ignorable="d"
>
  <Grid>
    <StackPanel Grid.IsSharedSizeScope="True">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="Header" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0">install</Label>
        <Grid Grid.Column="1">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBox Grid.Column="0" Text="{Binding InstallRootDirectoryPath, UpdateSourceTrigger=PropertyChanged}" />
          <Button Grid.Column="1" Command="{Binding SelectInstallRootDirectoryPathCommand}">select</Button>
        </Grid>
      </Grid>

      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="Header" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0">dirs</Label>
        <Grid Grid.Column="1" Grid.IsSharedSizeScope="True">
          <ItemsControl ItemsSource="{Binding IntellisenseDirectoryItems}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <StackPanel />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="DirectoryName" />
                    <ColumnDefinition Width="*" SharedSizeGroup="DirectoryVersion" />
                  </Grid.ColumnDefinitions>

                  <Label Grid.Column="0" Content="{Binding DirectoryName}" />

                  <ComboBox Grid.Column="1" ItemsSource="{Binding VersionItems}" SelectedItem="{Binding SelectedVersion}">
                    <ComboBox.ItemTemplate>
                      <DataTemplate>
                        <TextBlock Text="{Binding Version}" />
                      </DataTemplate>
                    </ComboBox.ItemTemplate>
                  </ComboBox>
                </Grid>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
          <!--<DataGrid AutoGenerateColumns="False" ItemsSource="{Binding IntellisenseDirectoryItems}">
            <DataGrid.Columns>
              <DataGridTextColumn
                Header="Directory"
                Binding=
              />
              <DataGridComboBoxColumn
                Header="VersionItems"
                ItemsSource="{Binding VersionItems}"
                SelectedItemBinding="{Binding SelectedVersion}"
              />
            </DataGrid.Columns>
          </DataGrid>-->
        </Grid>
      </Grid>

    </StackPanel>
  </Grid>
</Window>
